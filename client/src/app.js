/**
 * DEAD FILE — intentionally emptied.
 *
 * This held 2,033 lines of an unrelated hostel/PG management app — rooms, guests, rent due, balance sheets. Nothing imports it, so Vite never bundled it and it could
 * not reach a user, but it sat in the tree confusing anyone reading the repo and skewing every grep and line count.
 *
 * It cannot be DELETED: deployment is drag-and-drop onto GitHub, which adds and
 * overwrites files but never removes them, so a deletion here would leave the
 * original in place. Emptying it to this comment is the only way to retire it
 * without a separate explicit commit that deletes the path.
 */
