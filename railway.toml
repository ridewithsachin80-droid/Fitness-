[build]
builder = "nixpacks"
buildCommand = "bash build.sh"

[deploy]
startCommand = "node --max-old-space-size=512 server/startup.js"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[environments.production.variables]
NODE_ENV = "production"
PORT     = "3000"
